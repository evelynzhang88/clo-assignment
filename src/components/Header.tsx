import './Header.scss'

export default function Header() {
  return (
    <header className="app-header">
      <div className="header-content">
        <div className="logo">CONNECT</div>
        <button className="optional-feature-btn">OPTIONAL FEATURE</button>
      </div>
    </header>
  )
}

